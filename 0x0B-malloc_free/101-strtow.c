#include <stdlib.h>

/**
 * strtow - main function
 *
 * Desciption: 'Split a string into words'
 *
 * @str: string passed to the function
 *
 * Return: a pointer to an array of string of words
 */

char **strtow(char *str)
{
	int i, len = 0, words = 0;

	for (i = 0; str[i] != '\0'; i++)
	{
		len++;
		word
	}
}
