#include "main.h"

/**
 * infinte_add - main function
 *
 * Description: 'add two numbers that are strings'
 *
 * @n1: first number
 * @n2: secend number
 * @r: buffer where to store the result
 * @size_r: size of the buffer r
 *
 * Return: pointer to the buffer r or 0 if the result cant fit in r
 */

char *infinte_add(char *n1, char *n2, char *r, int size_r)
{
	int i, j, k
}
