#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'print to the screen using puts()'
 *
 * Return: Always 0
 */

int main(void)
{
	char str[] = "\"Programing is like building a multilingual puzzle";

	puts(str);
	return (0);
}
