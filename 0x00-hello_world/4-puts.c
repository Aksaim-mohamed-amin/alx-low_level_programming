#include <stdio.h>

/**
 * main - Print to the screen using puts()
 *
 * Return: Always 0
 */

int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";

	puts(str);
	return (0);
}
