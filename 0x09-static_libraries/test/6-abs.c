#include <stdlib.h>

/**
 * _abs - Entrey point
 *
 * Description: 'return the abs of a number using
 * the function abs()'
 *
 * @n: the number giving
 *
 * Return: abs(n)
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
